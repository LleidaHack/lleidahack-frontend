services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lleidahack-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    restart: unless-stopped
